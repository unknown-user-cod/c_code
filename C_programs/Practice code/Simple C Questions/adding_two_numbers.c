#include <stdio.h>

int main()
{
    int a,b;

    // accepting three inputs but only two inputs are given ???
    scanf("%d %d ", &b, &a);

    // d = a + b;

    printf("a+b = %d \n", a+b);

    return 0;
}
